# Tauri build artifacts
target/
Cargo.lock

# Mobile project scaffolding generated by `tauri android init` / `tauri ios init`.
# These ARE committed in some projects but contain absolute paths + signing
# config we don't want to track — regenerate locally.
gen/
